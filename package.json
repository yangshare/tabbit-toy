{
  "name": "tabbit2api",
  "version": "1.0.0",
  "description": "Reverse-proxy Tabbit browser AI backend into an OpenAI-compatible API (reverse-engineered)",
  "type": "module",
  "private": true,
  "scripts": {
    "probe": "node scripts/probe.mjs",
    "probe:chat": "node scripts/probe.mjs --step chat",
    "probe:models": "node scripts/probe.mjs --step models",
    "probe:nopro": "node scripts/probe.mjs --no-pro",
    "start": "node src/server.mjs"
  },
  "dependencies": {
    "undici": "^7.28.0"
  }
}
